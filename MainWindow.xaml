<Window x:Class="Мирхаликов_БПИ24_01_Лаба.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Лабораторная работа №1. Вариант 9 — Функции (sin, cos, tg)"
        Height="560" Width="720" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Иерархия классов: функция одной переменной (синус, косинус, тангенс)"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Значение x:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="XValueTextBox" Width="100" Text="1" VerticalAlignment="Center"/>
            <Button x:Name="ComputeButton" Content="Вычислить и показать отчёт"
                    Margin="12,0,0,0" Padding="10,4" Click="ComputeButton_Click"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReportTextBlock"
                           FontFamily="Consolas" FontSize="13"
                           Margin="10" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
